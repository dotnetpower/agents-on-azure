# AutoGen — Multi-Agent with Azure Event Hubs

Three AutoGen agents streaming events through Azure Event Hubs.

## Quick Start

```bash
cp ../../../.env.example .env
uv sync
uv run python src/main.py
```
